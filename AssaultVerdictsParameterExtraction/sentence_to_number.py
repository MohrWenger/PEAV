from AssaultVerdictsParameterExtraction import penalty_extraction as pe

# Receive a file of sentences.

# Decide a wining choice (argmax/ last/ all/... ).
def extract_from_multiple_per_file(db):
    # case_names = db[CASE_NAMES]
    # sentence_list = db[SENT_LIST]
    #
    # pe.from_sentence_list(case_names, sentence_list)
    pass

# Extract the number
# (if the winning chice is all then should extrect the number from each and then provide the choose).

# Send to validate.

if __name__ == "main":
    pass